# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Скрипты бэкапа держим вне репозитория: в них попадает пароль от базы.
# deploy.sh под правило не подпадает — секретов он не содержит, читает их из server/.env
backup*.sh

# Скриншоты и профиль браузера от проверок вёрстки через Playwright —
# рабочий мусор, в репозитории не нужны
.playwright-mcp/
chart-light.png

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
