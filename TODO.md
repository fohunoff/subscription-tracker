~~Добавить вариант подписки "раз в квартал"~~ — сделано
~~Добавить опционально "дата окончания"~~ — сделано: поле `endDate` в форме, после
наступления даты последний платёж пишется в историю (событие `payment`) плюс `ended`;
флаг `archiveOnEnd` (по умолчанию включён) отправляет подписку в архив, иначе она
остаётся в списке с пометкой «срок истёк» и не участвует в суммах и уведомлениях;
уведомление перед последним платежом об этом предупреждает
  - ~~кнопка завершить (типа, отписался или что-то типа того)~~
  - ~~после: уходит в архив, но сохраняя все настройки~~
    - ~~лог по подписке (что и когда редактировали)~~ — модель SubscriptionEvent + GET /api/subscriptions/:id/history

  Осталось по этой теме:
  - ~~показать историю изменений в интерфейсе~~ — сделано: в детальном окне подписки
  - ~~статистика "сколько уже потрачено" на основе лога~~ — сделано: раздел
    «Сколько уже потрачено» с графиком по месяцам (см. ниже)


??? Добавить версионность кода и вывод версии в подвал сайта для понимания источника истины багов


UI
- поправить тёмную тему
- ~~нужен поиск (по подпискам)~~ — сделано: по названию и категории, охватывает архив
- перетаскивание карточек между блоками категорий
- ~~создавать чек лист оплаченных подписок на текущий месяц~~ — решено не делать:
  подписки списываются сами, отмечать нечего, а заброшенный через два месяца
  чек-лист занижал бы статистику незаметно. Вместо него платежи пишутся в лог
  автоматически (см. ниже)
- ~~отображение по категориям / сплошным списком, переключатель в шапке~~ — сделано:
  сплошной список отсортирован по дате ближайшего платежа и сгруппирован по месяцам,
  годовые и квартальные встают между месячными в своём месяце; выбранный вид запоминается
- ??? пак популярных иконок для сервиса с поиском по названию
- ~~клик по плашке подписки выводит окно с информацией~~ — сделано: сводка
  (стоимость с пересчётом в базовую валюту, ближайший платёж, уведомления) + история изменений
- ~~нужна статистика: сколько уже потрачено (??? типа график)~~ — сделано:
  раздел «Сколько уже потрачено», `GET /api/stats/spending`, колонки по месяцам
  (свой SVG), топ подписок, табличный вид, периоды 6 / 12 / 24 месяца
  - ~~брать сумму от даты создания с учётом изменений?~~ — считается от даты
    старта подписки по событиям `payment` с ценой на момент списания
  - ~~Пересчитывать общую сумму трат после смены даты старта и сохранения~~ —
    сделано: правка даты старта или цикла пересобирает оценочные платежи сама,
    в тосте видно, сколько записей перестроено
  - ~~платежи пишутся в лог сами (событие `payment` с ценой и валютой на момент
    списания), считать нужно по ним, а не по текущей цене~~
  - ~~курс брать на дату платежа: `CurrencyRate` пишется новой записью раз в час,
    история есть с момента запуска приложения~~
  - ~~прошлое (до появления автозаписи) — разовым бэкфиллом по логу изменений,
    такие записи помечать оценочными~~ — сделано:
    `server/scripts/backfill-payments.js` (`--apply`, `--reset`), записи
    с `estimated: true`; на сервере ещё не запускался
- ~~в блоке общей инфы: `Всего подписок: N / Ближайшая: имя (категория) дата`~~ — сделано:
  клик по имени открывает детальное окно, клик по категории скроллит к ней
- ~~лоадеры (скелетоны) на блоки~~ — сделано: категории, подписки, архив, блок расходов



~~перенос в архив: если перенесли -- то это дата окончания подписки. Если вернули из
архива до даты следующего платежа -- то просто "вернули", если вернули из архива после
даты следующего платежа -- то это "восстановление" подписки~~ — сделано: события
`returned` / `restored`, дата пропущенного платежа в логе и в тосте. Даты платежей
намеренно не сдвигаются — сервер не знает, возобновили подписку или оформили заново


- ~~нужно поменять логику вывода общей стоимости -- выводить в той валюте, в которой
  подписка (типа `4000 RSD / 32000 Rub / 2000 Eur`) -- сортировка валют в алфавитном
  порядке~~ — сделано: `CurrencyBreakdown` в блоке расходов и в заголовке категории,
  `totalsByCurrency` в `/api/stats`
- ~~в настройках указываем валюту по умолчанию~~ — сделано: настройка `defaultCurrency`,
  подставляется в форму новой подписки, на существующие не влияет


# Инфраструктура

- переименовать папку статики `/var/www/fohunoff/data/www/tracker.fohunoff.ru` → `.com`
  (домен переехал на tracker.fohunoff.com, папку намеренно оставили со старым именем;
  при переименовании поправить `root` в конфиге Nginx и `DEPLOY.md`)

# Деплой

Одна команда на сервере, подробности — в `DEPLOY.md`:

    cd /home/fohunoff/repos/subscription-tracker
    git pull origin main
    ./deploy.sh
