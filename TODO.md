Добавить вариант подписки "раз в квартал"
Добавить опционально "дата окончания"
  - кнопка завершить (типа, отписался или что-то типа того)
  - после: уходит в архив, но сохраняя все настройки (уведомления, категорию и тд и тп, чтобы можно было без проблем её восстановить)
    - но тут тогда какой-то лог нужен по подписке (когда редактировали и что, типа -- цена, категория (???), дата (???)) -- пригодится в статистике


UI
- нужен поиск (по подпискам)
- перетаскивание карточек между блоками категорий
- создавать чек лист оплаченных подписок на текущий месяц
- нужно отображение как по категориям, так и сплошным списком (??? сначала все месячные, потом годовые) - переключался в шапке -- отображение:
  - ??? выводим помесячные в зависимости от даты + если попадается дата годовой подписки на текущий месяц - то вставляем её между месячными подписками -- остальные годовые просто по порядку ??
- ??? пак популярных иконок для сервиса с поиском по названию
- клик по плашке подписки выводит окно с информацией (??? с какой)
- нужна статистика: сколько уже потрачено (??? типа график)
- в блоке общей инфы -- `Всего подписок: 14 / Ближайшая: имя (категория) дата` - клик по имени ведёт к категории (или открывает детальную инфу ??? )
- лоадеры (скелетоны) на блоки



- нужно поменять логику вывода общей стоимости -- выводить в той валюте, в которой подписка (типа `4000 RSD / 32000 Rub / 2000 Eur`) -- сортировка валют в алфавитном порядке
- в настройках мы просто указываем валюту по умолчанию (чтобы она автоматом была выбрана в выпашаке при добавлении новой подписки)


# Инфраструктура

- переименовать папку статики `/var/www/fohunoff/data/www/tracker.fohunoff.ru` → `.com`
  (домен переехал на tracker.fohunoff.com, папку намеренно оставили со старым именем;
  при переименовании поправить `root` в конфиге Nginx и `DEPLOY.md`)

# Деплой

Актуальная процедура — в `DEPLOY.md`. Кратко (выполняется на сервере):

    cd /home/fohunoff/repos/subscription-tracker
    git pull && npm run build
    sudo cp -r dist/* /var/www/fohunoff/data/www/tracker.fohunoff.ru/
    sudo chown -R fohunoff:fohunoff /var/www/fohunoff/data/www/tracker.fohunoff.ru